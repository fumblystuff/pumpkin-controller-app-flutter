import 'package:logging/logging.dart';

const appName = 'Pumpkin Controller';
const docUrl =
    'https://github.com/fumblystuff/pumpkin-controller-app-flutter/wiki';

const udpPort = 65000;
const udpBroadcastPort = 65001;

final log = Logger(appName);
